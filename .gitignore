backend/.env
**/.env
**/.env.local
**/.env.*.local

# Dependencies and build output (all packages)
node_modules
dist
*.log
.DS_Store

# Local DB (use migrations / seed; do not commit real data)
backend/data/*.db
# SQLite hot backups (timestamped copies)
backend/data/backups/

.vercel
.env*

# Local signal simulator CSV exports (repo root)
/signals-*.csv

# Internal-only docs (not for repo / partners)
docs/TECH_DEBT.md
docs/partners/OUTBOUND_LIST.md
