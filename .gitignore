backend/.env
**/.env
**/.env.local
**/.env.*.local

# Dependencies and build output (all packages)
node_modules
dist
*.log
.DS_Store

# Local DB (use migrations / seed; do not commit real data)
backend/data/*.db
# SQLite hot backups (timestamped copies)
backend/data/backups/

# Supabase CLI
supabase/.temp/

# Vendored copy of monorepo `shared/` for backend-only deploys (see backend/scripts/sync-shared.cjs)
backend/shared/
