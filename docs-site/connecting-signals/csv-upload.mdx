---
title: CSV upload
description: Import signal rows from a spreadsheet.
---

Use CSV when you don't have an API connector yet.

1. **Settings → Signal Sources → Connected → Import CSV**
2. Include a column that matches your release **version** string
3. Signal columns must match defined signal ids

Verdikt applies rows to matching releases and evaluates thresholds on the next gate check.
